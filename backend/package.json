{
  "name": "route-planner-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:init": "docker exec -i smart-traffic-route-planner-postgres-1 psql -U route_user -d route_db < src/db/schema.sql",
    "db:seed": "docker exec -i smart-traffic-route-planner-postgres-1 psql -U route_user -d route_db < src/db/seed.sql"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}